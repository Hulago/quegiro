const APP = {
  NOT_FOUND: 'AppNotFound',
  UNAUTHORIZED: 'AppUnauthorized'
};

const PROFILE = {
  PROFILE: 'Profile'
};

const DASHBOARD = {
  DASHBOARD: 'Dashboard'
};

export const COMMON_ROUTES = {
  APP,
  DASHBOARD,
  PROFILE
};
